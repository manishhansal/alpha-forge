/**
 * Constants shared between server (page loaders, journal queries) and
 * client (journal-data-context) for the India paper-trading journal.
 *
 * Keep the page size matched to the crypto journal so the layout, page
 * count and "showing X–Y of Z" footer behave identically across markets.
 */

export const INDIA_JOURNAL_PAGE_SIZE = 10;
